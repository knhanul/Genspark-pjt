# 외부 접속 설정 가이드

## 🌐 외부에서 접속하기

현재 설정: `knhanul.duckdns.org`

### 1. 방화벽 포트 오픈 (필수)

```bash
# 포트 3000 (백엔드) 오픈
sudo ufw allow 3000/tcp

# 포트 3001 (프론트엔드) 오픈
sudo ufw allow 3001/tcp

# 방화벽 상태 확인
sudo ufw status
```

### 2. 환경 변수 설정

`client/.env` 파일 수정:

```env
# 로컬 접속
REACT_APP_API_URL=http://localhost:3000/api

# 외부 접속
REACT_APP_API_URL=http://knhanul.duckdns.org:3000/api
```

### 3. CORS 설정 (이미 적용됨)

백엔드 서버는 이미 모든 origin을 허용하도록 설정되어 있습니다.

### 4. 서버 실행

```bash
# 모든 인터페이스에서 접속 허용
./run-app-simple.sh
```

### 5. 접속 주소

**로컬 (서버 내부)**
- 백엔드: http://localhost:3000
- 프론트엔드: http://localhost:3001

**외부 (인터넷)**
- 백엔드: http://knhanul.duckdns.org:3000
- 프론트엔드: http://knhanul.duckdns.org:3001

---

## 🔧 문제 해결

### 외부 접속이 안 되는 경우

#### 1. 방화벽 확인

```bash
# UFW 방화벽 상태
sudo ufw status

# 포트가 열려있는지 확인
sudo netstat -tuln | grep -E ':(3000|3001)'
```

#### 2. 서버가 0.0.0.0에서 리스닝하는지 확인

```bash
# 백엔드
sudo netstat -tuln | grep :3000

# 결과: 0.0.0.0:3000 (모든 인터페이스)
#       127.0.0.1:3000 (로컬만) ← 이러면 안됨!
```

#### 3. DuckDNS 설정 확인

브라우저에서 확인:
```
http://knhanul.duckdns.org
```

IP가 올바르게 설정되어 있는지 확인

#### 4. 라우터 포트 포워딩 (필요시)

만약 서버가 라우터 뒤에 있다면:
- 라우터 관리 페이지 접속
- 포트 포워딩 설정
  - 외부 포트 3000 → 서버 IP:3000
  - 외부 포트 3001 → 서버 IP:3001

---

## 📝 현재 설정 상태

### 백엔드 (server/index.js)
```javascript
app.listen(PORT, '0.0.0.0', () => {
  // 모든 인터페이스에서 접속 허용
});
```

### 프론트엔드 (client/.env)
```env
HOST=0.0.0.0  # 모든 인터페이스에서 접속 허용
REACT_APP_API_URL=http://knhanul.duckdns.org:3000/api
```

---

## 🚀 빠른 테스트

```bash
# 외부에서 백엔드 API 테스트
curl http://knhanul.duckdns.org:3000/api/health

# 결과:
# {"status":"OK","message":"팝송 학습 API 서버가 정상 작동 중입니다.","timestamp":"..."}
```

---

## ⚠️ 보안 주의사항

1. 프로덕션 환경에서는 HTTPS 사용 권장
2. 환경 변수에 민감한 정보 저장 금지
3. 방화벽 규칙 정기적으로 검토
4. DuckDNS 토큰 노출 방지
